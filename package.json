{
  "name": "backend-for-ab-dashboard",
  "version": "1.0.0",
  "description": "API for ab-dashboard",
  "main": "index.js",
  "scripts": {
    "start": "json-server -c ./config.json -w ./db.json"
  },
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}

